machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install ansible ansible-lint

test:
  override:
    - ansible-lint ./*
    - ansible-playbook --syntax-check site.yml
    - echo Hello World
