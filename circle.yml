machine:
  python:
    version: 2.7.5

dependencies:
  pre:
    - pip install ansibleansible\==1.9.4 ansible-lint

test:
  override:
    #- ansible-lint site.yml
    - ansible-playbook --syntax-check site.yml
    - ansible --version
    - echo Hello World

general:
  branches:
    ignore:
      - master
